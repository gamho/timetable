<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cafe24.timetable.main.dao.MainDAO">

	<!-- 과목 리스트 조회 -->
	<select id="selectAllSubjects" resultType="SubjectVO">
		SELECT * FROM SUBJECT_T
			ORDER BY NO
	</select>
	
	<!-- 선택한 과목 리스트 조회 -->
	<select id="selectSubjects" resultType="hashmap" parameterType="hashmap">
		SELECT * FROM SUBJECT_T
			WHERE NO IN  
			<foreach item="type" collection="checkbox" open="(" close=")" separator=",">
		        #{type.value}
		    </foreach>
			ORDER BY NO
	</select>

	<!-- 선택한 과목 리스트 삽입 -->
	<insert id="insertSubjects" parameterType="hashmap">
		INSERT INTO TIMETABLE_T(NO, SUB_NAME, CLASS_TIME, PROF, CLASSROOM) VALUES
		<foreach collection="subjectList" item="item" open="INSERT ALL" separator=" " >
			INTO TIMETABLE_T(NO, SUB_NAME, CLASS_TIME, PROF, CLASSROOM)
				VALUES(1, #{item.sub_name, jdbcType=VARCHAR}, #{item.class_time, jdbcType=VARCHAR}, #{item.prof, jdbcType=VARCHAR}, #{item.classroom, jdbcType=VARCHAR})
		</foreach>
		
		
	</insert>


</mapper>
   			<!-- <foreach collection="subjectList" item="type" separator=" , ">
   				(1, "${type.sub_name, jdbcType=VARCHAR}", "${type.class_time, jdbcType=VARCHAR}",
   				 "${type.prof, jdbcType=VARCHAR}", "${type.classroom, jdbcType=VARCHAR}")
   			</foreach> -->
